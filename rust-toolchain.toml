# The RP2350's Cortex-M33 cores are a first-class upstream Rust target,
# so plain stable works; rustup fetches the target automatically.
[toolchain]
channel = "stable"
targets = ["thumbv8m.main-none-eabihf"]
components = ["rustfmt", "clippy"]
